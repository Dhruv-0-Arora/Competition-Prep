#include <bits/stdc++.h>
using namespace std;

void solve()
{
    int n, k; cin >> n >> k;
    string s; cin >> s;
    string newStr[n], past[k];

    for (int i = 0; i < k; i++) past[i] = s[i];

    for(int i = k-1; i < n; i++)
    {
        // if s[i] not in past, 
    }
}

int main()
{
    short t; cin >> t;
    while (t--) solve();
}