#include <bits/stdc++.h>
using namespace std;

void solve()
{
    int a, b; cin >> a >> b; // the coordinates of the lattice point where Mary lives.

    cout << 2 << endl;
    cout << a - 1 << " " << 1 << endl;
    cout << a << " " << b << endl;

}

int main()
{
    int t; cin >> t;
    while (t--) solve();
}