#include <bits/stdc++.h>

using namespace std;

void solve() {
    int n, k, x; cin >> n >> k >> x;

    if (k == 1 && x == 1) {
        cout << "NO" << endl;
        return;
    } else if (k == 2 && x == 1 && n != 2) {
        cout << "NO" << endl;
        return;
    }
}

int main() {
    int t; cin >> t;
    while (t--) {

    }
}